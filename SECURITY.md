# 安全说明

## 凭证与本机数据

本项目不提供任何可共用的行情、券商或模型凭证。请只使用你自己的授权，并遵守数据提供方的协议。

运行时数据默认保存在仓库下的 `.local/`，其中可能包含：

- 灵犀 `gtht-entry.json`；
- 长桥 OAuth 客户端及令牌；
- 本机数据库、持仓、行情缓存和日志；
- 下载的数据源组件与安装包。

`.local/` 已被 Git 忽略。不要用“上传整个文件夹”或压缩目录的方式发布项目；应当只通过 Git 发布已跟踪文件。提交前运行：

```sh
python3 scripts/check_no_secrets.py --history
```

如果凭证曾出现在聊天、截图、日志、Issue、提交或其他外部位置，应立即在对应数据提供方撤销并重新生成。仅从最新代码删除凭证不能清除 Git 历史。

## 报告安全问题

请通过 GitHub 仓库的 **Security → Report a vulnerability** 私下报告。不要在公开 Issue 中附带密钥、账户资料、持仓、交易记录或可复现的真实凭证。

## 使用边界

程序只监听本机地址，券商连接保持只读，不提供真实下单接口。任何贡献若改变监听范围、凭证存储、外部请求、账户权限或交易行为，都需要单独说明并增加相应测试。

## ChatGPT 选股工作台

“GPT 选股”只在本机生成可复制任务并解析用户主动粘回的答案，不使用 OpenAI API，不要求或保存 OpenAI API Key，也不自动操控 ChatGPT 网页。当日提示词、完整答案和可选的 `chatgpt.com` 对话链接保存在本机数据库；收盘后完整问答和账户上下文会清除，只留下脱敏结构化结论与本地复盘指标。系统事件不记录提示词、回答、持仓或账户内容。

账户选项默认关闭。开启后，券商适配器仅返回白名单字段：净资产、现金、购买力、可用资金、保证金风险、更新时间以及最多40只持仓。账户号、订单号、OAuth 资料、令牌、密钥和内部路径不会进入响应。刷新失败时仅复用上次成功的脱敏本机快照，并显式标记过期。

用户点击复制后，预览中的文本将离开本机进入 ChatGPT，应在复制前再次检查。导入的回答被视为不可信外部文本：服务只解析 `SHORTLIST_RESULT_V1` 标记内的JSON，采用字段白名单，不执行HTML、链接、脚本或其中的操作指令。全市场任务只打开官方 Longbridge App 页面，不会把 ChatGPT 授权回传给本地服务；连接和授权仍以 ChatGPT 内显示为准。

GPT候选不能直接成为买入信号。每只候选都必须重新通过本机证券范围、同源行情、实时报价、盘口、不追价、失效线、持仓名额和A股交易限制。Longbridge及盈透连接继续只读；GPT任务明确禁止下单、改单、撤单和最大可买量等交易工具。
